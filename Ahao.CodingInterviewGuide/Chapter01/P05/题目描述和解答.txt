用一个栈实现另一个栈的排序

题目：
一个栈中元素的类型为整型，现在想将该栈从顶部到底部按从大到小的顺序排序，只许申请一个栈。

解答：
将要排序的栈记为stack，申请的辅助栈记为help。在stack上执行pop操作，弹出的元素记为cur
——如果cur小于或等于help的栈顶元素，则将cur直接压入help
——如果cur大于help的栈顶元素，则将help的元素逐一弹出，逐一压入stack，知道cur小于或等于help的栈顶元素，再将cur压入help
